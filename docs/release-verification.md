# Release verification

`tutorials/efficientnet_classification_colab.ipynb` (`E2E`, **standalone** carrier) is a **release candidate** until the exact notebook revision has executed top-to-bottom in a clean supported runtime. Unit tests, the tiny-model test, JSON validation, code-cell compilation, the generator parity checks and `tools/validate_release_assets.py` are necessary checks but are **not** runtime evidence under DIMER Notebook Specification 2.1 (REL8). This file is the durable release-gate record for the notebook.

The upstream snapshot is pinned to `1b5383e5f79cc0f7fc067e372f8f26a5fa73f26a` and the notebook is regenerated with that revision and manifest, so it can run. One default-path execution is recorded below (2026-09-26, Kaggle T4); release step 7 (REL12) has not been exercised, so the status stays `Candidate`.

## Automatic coverage (static and unit, every pull request)

CI installs the pinned CPU-only torch wheel and the other runtime pins, then runs:

- `ruff check src tests tools`;
- `pytest`: snapshot verification and staging against synthetic manifests, the unpinned refusals, the import boundary, input and dataset validation, BYOD directory reading, average precision, the evaluation report, the notebook parity checks, the weight-facts check, the pin tool against a fake Hub, the data module against synthetic archives and folders (member-path refusals, size bounds, digest verification, stratified splits), and `tests/test_small_model.py` — the random-weight `efficientnet_b0` architecture at a 64 px input taken through `predict`, `zero_shot_evaluate`, full and frozen `finetune` (including the check that a frozen backbone keeps its weights and BatchNorm statistics), `evaluate`, `save_artifact` and `apply_artifact`;
- `tools/validate_release_assets.py`: model card 1.2 structure and front matter, the pin state across the package, the manifest and the documents, identity consistency, the weight facts, the release-status tokens, and the notebook's structure, carried modules, parity, markers, BYOD gates and location fields;
- `tools/build_notebook.py --check`.

These are source, provenance and unit checks. None of them loads the pinned checkpoint, so none of them is execution evidence.

## Supported release verification procedure

Before changing the registry status from `Candidate` to `Release-grade`:

1. confirm the snapshot is pinned (`MODEL_REVISION` is a 40-hex commit and every manifest entry has a SHA-256) and that static CI is green on the exact commit under review;
2. open that exact notebook revision in a new GPU runtime (Colab or Kaggle, Tesla T4 or similar) with **no repository checkout** and a clean model cache;
3. run the notebook top-to-bottom without editing implementation cells, with every form field at its default (`USE_BYOD_IMAGE = False`, `USE_BYOD_DATASET = False`, `PER_CLASS = 200`, `EPOCHS = 5`, `FREEZE_BACKBONE = False`);
4. verify that Section 1 reports `NOTEBOOK_SOURCE.repository_revision` equal to `metadata.dimer.generated_from.revision`, and that the installed versions equal the inline `PINS`;
5. verify that every default-path stage completes: the pinned install; the carried modules; staging of all three manifest entries and `verify_snapshot`; the sample archive download with its size and SHA-256 check; `validate_dataset`; the three-way split with its leakage assertion; `validate_inputs` with the `top_k=0` rejection finding; ImageNet top-5 predictions and the `sample-sanity` report; the zero-shot baseline; the blank and noise probes; the untrained-head baseline; `finetune`; the held-out comparison; the unseen split; adapter export, reload and the tolerance check; and the five outputs in `outputs/`;
6. record the number of images per class read from the archive, the held-out accuracy and balanced accuracy of the majority-class baseline, the zero-shot mapping, the untrained head and the fine-tuned model, the unseen-split scores, the per-epoch losses, the trainable and total parameter counts, the degenerate-probe answers and the wall time in the table below. No metric value is asserted in advance: a fine-tuned score at or below the zero-shot baseline is a finding to record, not a failure by itself;
7. exercise the BYOD dataset branch once with a small directory or `.zip` of class folders (`BYOD_DATASET_PATH`) and once with an incompatible one (for example a class with a single image), and record both outcomes (REL12);
8. record the notebook Git blob id, commit, runtime (platform, GPU, Python, PyTorch, torchvision, timm), model identifier and revision, the sample archive's SHA-256, whether the model cache was clean, and any warning judged harmless with the reason;
9. record no access tokens or other secrets.

A known-failing default path in the supported runtime blocks release.

## Recorded executions

Notebook identity is the Git blob id of `tutorials/efficientnet_classification_colab.ipynb` (verify with `git rev-parse <commit>:tutorials/efficientnet_classification_colab.ipynb`). Each record uses the fields Date, Subject, Runtime, Procedure, Observed result and Caveats.

| Date (UTC) | Subject (commit / notebook blob) | Runtime | Procedure | Observed result | Caveats |
|---|---|---|---|---|---|
| 2026-09-26 (kernel 2026-09-26 00:19:45 – 00:23:21) | commit `9dce015` / blob `fbcb57dc1362` (`NOTEBOOK_SOURCE.repository_revision` `0d8c5c6`, the source revision the notebook was generated from, equal to `metadata.dimer.generated_from.revision`; `0d8c5c6..9dce015` changes only the notebook and `tests/test_pin_snapshot.py`) | Kaggle Tesla T4 (`kurtvalcorza/dimer-nb2-efficientnet-classification` v1), batch run; image `gcr.io/kaggle-gpu-images/python@sha256:37c64f7dd9c54116ecd1bcc88817c5469b88387388fade02bfa8bf3fc647d461` (image torch 2.10.0+cu128), Python 3.12.13, Tesla T4 15360 MiB, driver 580.159.04; after the inline pins torch 2.14.0+cu130 (CUDA 13.0), torchvision 0.29.0+cu130, timm 1.0.29, numpy 2.5.3, Pillow 11.3.0 (equal to `PINS`); device `cuda:0`, float32 | serial suite: blob fetched at the 40-char SHA and Git-blob verified, model cache clean at start, no repository checkout; top-to-bottom with every form field at its default (`USE_BYOD_IMAGE = False`, `USE_BYOD_DATASET = False`, `PER_CLASS = 200`, `EPOCHS = 5`, `FREEZE_BACKBONE = False`, `SEED = 0`, `TOP_K = 5`); `timm/efficientnet_b0.ra_in1k` at `1b5383e5f79cc0f7fc067e372f8f26a5fa73f26a` (apache-2.0), 3 manifest files (21,360,641 B) staged and `verify_snapshot` passed (`verified_files` 3); sample archive `Cleanlab/cifar-10-subset` @ `bb5a7aabf1d1` (mit), 986,707 B, sha256 `66f90a4f87d865e8eb653b62f10e754684075a32314177de76832349d4b1fb19`, fetched and verified | **PASSED (default path)** — 212.1 s wall (pass 1 170.0 s stopped after the install cell with `RuntimeError: Core dependencies changed while older modules were loaded` (cuda-bindings 12.9.4 → 13.4.3, numpy 2.0.2 → 2.5.3), kernel restarted after the install cell as the notebook instructs; pass 2 42.0 s), 14/14 post-restart code cells ok. `validate_dataset`: 400 records, 200 images per class (`frog`, `truck`), 32×32 px, accepted with the finding of **100 groups of pixel-identical images**. Split (seed 0): train 280 / held-out 60 / unseen 60, train majority `frog`, ID leakage assertion passed. `validate_inputs` accepted the 4-image sample with the `top_k=0` rejection finding. ImageNet top-5 on the 4 sample images: frogs → `pick` 0.390 and `platypus` 0.121 (no frog class at top-1 or top-5), trucks → `moving van` 0.536 and `passenger car` 0.247; `sample-sanity` group hit rate top-1 0.25, top-5 0.5. Held-out (60 images, 30 per class) accuracy / balanced accuracy: majority class 0.500 / 0.500; zero-shot ImageNet mapping 0.9667 / 0.9667 (recall frog 0.9333, truck 1.0; confusion [[28, 2], [0, 30]]; mean group mass 0.2643); untrained two-class head 0.5167 / 0.5167 (recall frog 0.1667, truck 0.8667); fine-tuned 1.000 / 1.000 (confusion [[30, 0], [0, 30]], mean top-1 score 0.9867). Unseen (60 images): fine-tuned 1.000 / 1.000, mean top-1 score 0.9787. Degenerate probes, ImageNet head: blank → `can opener` 0.011, `envelope` 0.009, `corkscrew` 0.009; noise → `rapeseed` 0.036, `pole` 0.01, `nematode` 0.008; **adapted head: blank → `truck` 0.509 (`frog` 0.491), noise → `truck` 1.0**. `finetune`: full model, 4,010,110 of 4,010,110 parameters trainable, 5 epochs, batch 16, AdamW lr 0.0001, weight decay 0.01, no augmentation; epoch losses 0.4823, 0.1611, 0.0719, 0.0346, 0.0166; fine-tune cell about 9 s. Adapter `efficientnet_adapter.safetensors` 16,241,992 B, sha256 `56680dcf4070…`, 360 tensors, base state digest `d569899762ea…`; reload check 60 images, tolerance 0.0001, equivalent. BYOD refusals shown as expected (non-image object → `TypeError`; class with one image → `ValueError`). Five outputs written, sha256 `efficientnet_classification_result.json` `dc00963b3f64…`, `…_evaluation_report.json` `b1c41458dcdb…`, `…_input_manifest.json` `332148940790…`, `…_predictions.csv` `8411613d9971…`, adapter `56680dcf4070…` | One seeded split of 60 held-out and 60 unseen CIFAR-10 thumbnails (32 px, upsampled to 224 px), one runtime, one pass, no dispersion estimate. The zero-shot ImageNet mapping already scores 0.9667 on held-out (2 frogs missed); the fine-tuned 1.000 is two images more, within the resolution of a 60-image split (one image is about 1.7 points), so this run shows no measurable fine-tune gain. The archive holds 100 pixel-identical groups (an `original_images` file and its same-numbered `darkened_images` file) and `split_dataset` is not duplicate-aware, so held-out and unseen images can have exact copies in the training split: by the IDs in `…_predictions.csv`, 46 of 60 held-out and 36 of 60 unseen images have their same-numbered counterpart (`original_images` ↔ `darkened_images`) in the training split (the counterpart is in neither evaluation split; a count of counterparts, not of confirmed pixel copies). The adapted head has no reject option: a blank image split 0.509 / 0.491 between `truck` and `frog`, and pure noise got `truck` 1.0. Both frog sample images got non-frog ImageNet top-1 labels (`pick`, `platypus`) and one truck got `passenger car`. The pip resolver's conflict warnings for preinstalled Kaggle packages were harmless (the notebook's version check passed after the restart). **REL12 not exercised** (step 7: BYOD dataset branch with a small class-folder directory and with an incompatible one), so the status stays `Candidate`. No tokens or secrets recorded. |
