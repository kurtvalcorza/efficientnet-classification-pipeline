# Release status

Current status: **Candidate — default-path execution recorded on 2026-09-26 (Kaggle T4); REL12 BYOD exercise pending before promotion**. The pipeline package, the offline test suite (including the random-weight `efficientnet_b0` architecture at a 64 px input run through prediction, the zero-shot baseline, full and frozen fine-tuning, evaluation and adapter reload), `MODEL_CARD.md` (MODEL_CARD_SPEC 1.2) and the standalone `E2E` tutorial `tutorials/efficientnet_classification_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.1) exist. The upstream snapshot is pinned: `MODEL_REVISION` is `1b5383e5f79cc0f7fc067e372f8f26a5fa73f26a` and the manifest records every file's byte size and SHA-256 (`model.safetensors` `d569899762ea…`, matching the Hub's LFS record). The exact commit `9dce015` / notebook blob `fbcb57dc1362` executed top-to-bottom on Kaggle Tesla T4 on 2026-09-26 UTC with both BYOD branches off (212.1 s, 14/14 post-restart code cells): on 60 held-out CIFAR-10 thumbnails the fine-tuned head scored accuracy 1.000 against zero-shot ImageNet mapping 0.9667, untrained head 0.5167 and majority baseline 0.500, so the fine-tune shows no measurable gain over zero-shot on this split, and the tutorial split is not aware of the sample archive's 100 pixel-identical groups (46 of 60 held-out and 36 of 60 unseen images have their same-numbered counterpart (`original_images` ↔ `darkened_images`) in the training split). The full record is in `docs/release-verification.md`. Release step 7 (REL12: the BYOD dataset branch with a small class-folder directory and with an incompatible one) has not been exercised, so the status stays Candidate.
