# Release status

Current status: **Candidate — source complete, snapshot pinned, no execution recorded**. The pipeline package, the offline test suite (including the random-weight `efficientnet_b0` architecture at a 64 px input run through prediction, the zero-shot baseline, full and frozen fine-tuning, evaluation and adapter reload), `MODEL_CARD.md` (MODEL_CARD_SPEC 1.2) and the standalone `E2E` tutorial `tutorials/efficientnet_classification_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.1) exist. The upstream snapshot is pinned: `MODEL_REVISION` is `1b5383e5f79cc0f7fc067e372f8f26a5fa73f26a` and the manifest records every file's byte size and SHA-256 (`model.safetensors` `d569899762ea…`, matching the Hub's LFS record). No execution with the pinned weights is recorded in `docs/release-verification.md`, so the repository is not release-grade.
